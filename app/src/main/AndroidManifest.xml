<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calebfoo.split">

    <uses-permission android:name="android.permissions.READ_CONTACTS" />
    <uses-permission android:name="android.permissions.WRITE_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PopActivity"
            android:label="@string/title_activity_pop"
            android:theme="@style/AppTheme.PopMe"></activity>
        <activity android:name=".PopChooseActivity"
            android:label="PopChooseActivity"
            android:theme="@style/AppTheme.PopMe"></activity>
    </application>

</manifest>